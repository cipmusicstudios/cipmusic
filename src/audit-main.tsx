import React from 'react'
import ReactDOM from 'react-dom/client'
import AuditGallery from './AuditGallery'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AuditGallery />
  </React.StrictMode>,
)
